- var timestamp = Date.now();
- var version = Math.floor(timestamp / 1000);

link(rel="stylesheet" href=`css/module__reset.min.css?v=${version}`)
link(rel="stylesheet" href=`css/module__splide.min.css?v=${version}`)
link(rel="stylesheet" href=`css/module__base.min.css?v=${version}`)
